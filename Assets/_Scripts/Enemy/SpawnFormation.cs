using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnFormation : MonoBehaviour
{
    //  public void MoveToPath()
    //  {
    //      Vector3 nearestPointOnPath = pathCreator.path.GetPointAtDistance(pathCreator.path.GetClosestDistanceAlongPath(transform.position));
    //      displacement = transform.position - nearestPointOnPath;  // Vector3 between enemy and closest point of path
    //      if ((Mathf.Abs(displacement.x) && Mathf.Abs(displacement.z)) >= 0.5f)
    //      {
    //          transform.position += displacement.normalized * speed * Time.deltaTime;
    //      }
    //      else
    //      {
    //          initialDirection = -1 * player.GetComponent<Player>().getOrientation();
    //          onPath = true;
    //      }
    // }
}
